# Rebiz: data access - api

Calls to our Next.js Backend

In here we implement our business logic and access control.

## Access control

This is done in the `api` handlers

## Future SDK

Note that `queries` and `network` can be generated into an SDK at a later stage, so please keep them all as generic as possible, and follow the existing patterns for them.
