// Utility functions for UI components
export const withSx = (component: any) => {
  return (sx: any) => {
    return component;
  };
};

// Add other utility functions as needed
export const createStyledComponent = (baseComponent: any) => {
  return baseComponent;
};
